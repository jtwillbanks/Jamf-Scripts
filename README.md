# Jamf-Scripts

# Allow Standard Users to Add Printers
Allows standard users to add printers without admin authorization

# Set Automatic Time Zone
Force set automatic time zone and reset permissions

# TCC Checker
Checks user adjusted PPPC Preferences that are viewable in the TCC Database can use a search term like "Chrome" or "Terminal" or a full bundleID like "com.google.Chrome"
